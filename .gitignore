# Secrets — never commit.
.env
.env.local
.env.*.local
.env.development.local
.env.production.local

# Gateway state: account balances, session tokens, and the Arc master seed that every
# deposit address is derived from.
gateway/state.json
gateway/state.json.tmp

# Generated output of the Python site build.
public/

# Python
__pycache__/
*.py[cod]

# Node / Next.js — dependencies and build output, both reproducible from the lockfile.
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.vercel
